% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splist_auxiliar.R
\name{.splist_formated}
\alias{.splist_formated}
\title{Formateo de lista de especies en estructura taxonómica estandarizada}
\usage{
.splist_formated(splist, col_name = var_name)
}
\arguments{
\item{splist}{Un vector de caracteres o un \code{data.frame} que contiene los nombres científicos
de especies.}

\item{col_name}{Nombre de la columna (como cadena de texto sin comillas) en \code{splist} que contiene los nombres
científicos, en caso de que \code{splist} sea un \code{data.frame}. Por defecto toma \code{var_name},
una variable esperada en el entorno del paquete.}
}
\value{
Si \code{splist} es un vector, devuelve un \code{data.frame} con las columnas derivadas de los componentes
taxonómicos del nombre científico. Si es un \code{data.frame}, devuelve el mismo objeto con columnas
adicionales generadas por \code{.splist_format_df()} y un índice de orden (\code{sorter}) agregado.
}
\description{
Esta función interna toma una lista de nombres científicos de especies
(como vector de caracteres o \code{data.frame}) y los convierte en una estructura
tabular estandarizada que incluye sus componentes taxonómicos (género, epíteto, etc.),
además de verificar la validez del formato binomial.
}
\details{
Esta función aplica diferentes rutas de procesamiento dependiendo del tipo de entrada:
\itemize{
\item Si \code{splist} es un vector de caracteres, se clasifica con \code{.splist_classify()},
se transforma con \code{.transform_split_classify()} y se verifica con \code{.check_binomial()}.
\item Si \code{splist} es un \code{data.frame}, se procesa con \code{.splist_format_df()} usando la columna indicada
y luego se verifica el formato binomial. También se añade una columna auxiliar \code{sorter} para
conservar el orden original.
}
}
\keyword{internal}
