% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/textnear.R
\name{textnear}
\alias{textnear}
\title{Fuzzy Join Between Two Data Frames Based on String Distance}
\usage{
textnear(
  x,
  y,
  by.x,
  by.y,
  type = "inner",
  method = "osa",
  maxDist = 4,
  weight = c(d = 1, i = 1, s = 1, t = 1),
  p = 0,
  bt = 0,
  q = 1,
  nomatch = NA_integer_,
  matchNA = TRUE,
  useBytes = FALSE,
  nthread = getOption("sd_num_thread", 1L),
  suffixes = c(".x", ".y")
)
}
\arguments{
\item{x}{A data frame or data.table. The left table in the join operation.}

\item{y}{A data frame or data.table. The right table in the join operation.}

\item{by.x}{Character string. Name of the column in \code{x} to use for matching.}

\item{by.y}{Character string. Name of the column in \code{y} to use for matching.}

\item{type}{Character string specifying the join type. One of:
\itemize{
\item \code{"inner"}: Returns only matching rows from both tables (default)
\item \code{"left"}: Returns all rows from x, matching rows from y
\item \code{"right"}: Returns all rows from y, matching rows from x
\item \code{"full"}: Returns all rows from both tables
\item \code{"semi"}: Returns rows from x that have a match in y (no columns from y)
\item \code{"anti"}: Returns rows from x that have NO match in y
}}

\item{method}{Character string. Distance metric for string matching. Options include:
\code{"osa"} (Optimal String Alignment, default), \code{"lv"} (Levenshtein),
\code{"dl"} (Damerau-Levenshtein), \code{"hamming"}, \code{"lcs"}, \code{"qgram"}, \code{"cosine"},
\code{"jaccard"}, \code{"jw"} (Jaro-Winkler), \code{"soundex"}. See \code{stringdist::stringdist}
for details.}

\item{maxDist}{Numeric. Maximum allowed distance for a match. Strings with distance
greater than this value will not match. Default is 4.}

\item{weight}{Numeric vector with names \code{d}, \code{i}, \code{s}, \code{t}. Weights for deletion,
insertion, substitution, and transposition operations. Default: \code{c(d=1, i=1, s=1, t=1)}.}

\item{p}{Numeric. Penalty parameter for Jaro-Winkler distance (0-0.25). Default is 0.}

\item{bt}{Numeric. Boost threshold for Jaro-Winkler distance. Default is 0.}

\item{q}{Integer. Size of q-grams for q-gram based distances. Default is 1.}

\item{nomatch}{Value to return when no match is found. Default is \code{NA_integer_}.}

\item{matchNA}{Logical. Should NA values match each other? Default is \code{TRUE}.}

\item{useBytes}{Logical. Should matching be done byte-by-byte rather than
character-by-character? Default is \code{FALSE}.}

\item{nthread}{Integer. Number of threads to use for parallel processing.
Default uses option \code{"sd_num_thread"} or 1 if not set.}

\item{suffixes}{Character vector of length 2. Suffixes to append to duplicate
column names from x and y. Default is \code{c(".x", ".y")}.}
}
\value{
A data.table with the joined result. Column naming follows data.table
merge conventions with the specified suffixes for duplicate names.
}
\description{
Performs fuzzy matching between two data frames using approximate string matching.
Similar to dplyr joins but allows for inexact matches based on string distance metrics.
Supports multiple join types (inner, left, right, full, semi, anti) and various
string distance algorithms.
}
\details{
The function uses \code{stringdist::amatch()} for approximate string matching and
\code{data.table} for efficient join operations.

\strong{Join Logic:}
\itemize{
\item For \code{right} joins, matches are computed from y to x (reverse direction)
\item For other joins, matches are computed from x to y (standard direction)
\item \code{full} joins combine matching rows plus unmatched rows from both tables
\item \code{semi} and \code{anti} joins return early without merging tables
}

\strong{Performance Note:} Fuzzy matching can be computationally expensive for large
datasets. Consider filtering data or using more restrictive \code{maxDist} values
for better performance.
}
\examples{
\dontrun{
# Inner join with default OSA distance
df1 <- data.frame(name = c("John Smith", "Jane Doe", "Bob Johnson"))
df2 <- data.frame(nombre = c("Jon Smith", "Jane Do", "Robert Johnson"))

result <- textnear(df1, df2, by.x = "name", by.y = "nombre", maxDist = 2)

# Left join with Jaro-Winkler distance
result <- textnear(df1, df2,
                   by.x = "name", by.y = "nombre",
                   type = "left",
                   method = "jw",
                   maxDist = 0.1)

# Anti join to find unmatched records
unmatched <- textnear(df1, df2,
                      by.x = "name", by.y = "nombre",
                      type = "anti",
                      maxDist = 3)
}

}
\seealso{
\code{\link[stringdist]{amatch}} for the underlying matching algorithm,
\code{\link[data.table]{merge}} for standard exact joins
}
